import 'package:flutter/widgets.dart';

class DetactionPage extends StatelessWidget {
  const DetactionPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
