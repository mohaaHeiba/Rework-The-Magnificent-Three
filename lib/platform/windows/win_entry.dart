import 'package:flutter/material.dart';

class WinEntry extends StatelessWidget {
  const WinEntry({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
